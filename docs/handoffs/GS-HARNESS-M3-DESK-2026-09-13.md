# GS harness — M3: the DESK panel (handoff packets; a ruling writes ONE pings row, then sweeps) + Lanes calibration nits

Read first: docs/handoffs/GS-HARNESS-FOUNDING-SPEC-2026-09-13.md (§3 "Desk (NEW)", §5 Data + integrity — the WRITE set is closed: a ruling calls gs-private `scripts/ping.sh`, nothing else writes; §7 M3 acceptance; §8(a) register — Desk reads as a FOLIO: one packet one leaf, the WHAT-TO-JUDGE card as a printed plate, ANNOTATE as an overlay sheet; amber = a packet waiting on Ray), gs-private rules/decisions-arrive-staged.md (the desk conventions: ~/Desktop/handoff/<GAME>-<GATE>-<date>/ folders, WHAT-TO-JUDGE.html, ANNOTATE.html, .ready-gate-passed / .replay-gate-passed stamps, START-HERE.html, README.txt; a ruling sweeps the packet to ~/Documents/session-artifacts/), the M1/M2 code + docs/handoffs/GS-HARNESS-REGISTER-PASS-2026-09-13.md, and the three vision verdicts recorded in the harvest (dust vs quiet-grey luminance too close in night; state WORD still carries colour — colour belongs on the counter only; elapsed set too heavy, reads as a telemetry column; amber leaking into a persistent tree/status accent in night; nested boxes in the detail folio).

## Deliverable
1. DESK panel (new activity-bar icon beside Lanes): lists every packet folder under `~/Desktop/handoff/` (resolve via os.homedir(); ignore START-HERE.html/README.txt/dotfiles), one LEAF per packet: folder name parsed into game / gate / date, the WHAT-TO-JUDGE.html rendered inline as the plate (sanitised HTML in a sandboxed webview iframe; no scripts from the card), a file list with sizes and types, an "Open folder in Finder" action, an "Open ANNOTATE" action (opens the file with the OS), gate stamps shown (.ready-gate-passed / .replay-gate-passed present or not). Amber badge count = packets waiting.
2. RULING capture: on a leaf, a single control "Record ruling…" opening a small form: verdict (free text, one line, Ray's words) + optional tags; on confirm it runs gs-private `scripts/ping.sh -s <session> -t "<game>,ray,ruling" "<GAME>-<GATE> — RULED (Ray)" "<verdict text>"` with the GENERALSTAFF_ROOT the extension already knows (never hardcode a path; never write the pings file directly), shows the returned row, and ONLY on exit 0 moves the packet folder to `~/Documents/session-artifacts/<same name>/` (rename; if the target exists, suffix `-2`). Nothing else may write anywhere. The session id: read from the newest `docs/sessions/*session*.md` filename in GENERALSTAFF_ROOT (sNNN) or ask in the form with that as the default.
3. Lanes calibration (the nits): counter colour only — the state word is plain text; dust vs quiet-grey separated by luminance in all six palettes (state it numerically in the CSS comment); elapsed/cap/launched as true marginalia (small, muted, edge); no amber on any persistent accent in night; detail folio without nested boxes (one plate, rules not boxes).
4. Tests: Desk model from a fixture directory tree (3 packets incl. one with stamps and one with no card), ruling flow with a stubbed ping.sh (asserts the exact argv, no write on non-zero exit, move only on zero), sanitiser drops scripts; Lanes calibration tests updated; `npm run check` green (94/94 + new).
5. Version 0.4.12; VSIX (vsce); regenerate the standalone proofs incl. a `GS-HARNESS-M3-PROOF-desk-paper.png` with the fixture packets; commit them. Nothing outside workbench-extension/ + this doc + proofs; never touch master, src-tauri/, credentials, or gs-private.

---

## Harvest (2026-09-13)

Workbench **0.4.12**. Desk panel + ruling path + Lanes calibration:

- Desk lists `~/Desktop/handoff/` packet folders as folio leaves (ignores START-HERE.html / README.txt / dotfiles); WHAT-TO-JUDGE.html as a sanitised sandboxed plate; ANNOTATE + Finder actions; `.ready-gate-passed` / `.replay-gate-passed` stamps; amber badge = packets waiting.
- Ruling calls `scripts/ping.sh -s <session> -t "<game>,ray,ruling" "<GAME>-<GATE> — RULED (Ray)" "<verdict>"` against the resolved GENERALSTAFF_ROOT; sweep to `~/Documents/session-artifacts/` only on exit 0 (suffix `-2` on collision).
- Lanes nits: counter colour only on chit border/unit; plain state word; dust/quiet ΔL documented in CSS; edge marginalia; detail plate without nested mono boxes.
- Proofs via `workbench-extension/scripts/render-m3-proofs.ts`: `GS-HARNESS-M3-PROOF-desk-paper.png` (+ regenerated register proofs).
- Tests: 101/101 (`desk` fixture tree + stubbed ping argv/sweep + sanitiser + Lanes calibration asserts).
