# Build output
src-tauri/target/
src-tauri/gen/
node_modules/
dist/
build/

# Tauri generated
src-tauri/Cargo.lock

# Logs
*.log
logs/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Local config / secrets
.env
.env.local

# Tauri local config override — contains machine-specific signing identity.
# Operator keeps src-tauri/tauri.local.conf.json locally; it is merged by
# Tauri 2 at build time and must NOT be committed (contributors build fine
# without it; they just won't have a signing identity, which is correct).
src-tauri/tauri.local.conf.json

# Claude Design — gsd-006 CD bundle extract. The design is integrated
# into src/; this is the raw claude.ai/design output kept for reference.
claude-design-output/
